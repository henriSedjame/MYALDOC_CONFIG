### SERVER DETAILS ###
server :
  port : 9999


### SECURITY DETAILS ###

security:
  oauth2:
    sso :
      loginPath : /login
    client:
      clientId: test
      clientSecret: test
      accessTokenUri: http://localhost:9000/auth/oauth/token
      userAuthorizationUri: http://localhost:9000/auth/oauth/authorize
      scope : scope
    resource:
      userInfoUri: http://localhost:9000/auth/user

zuul :
  sensitive-headers :
  routes :
    service-a :
      path : /service-a/**
      serviceId : service-a
      stripPrefix : false

### MESSAGEs D'ERREUR  ###

error :
  messages :
    tokenRetrievingError : Une erreur est survenue lors de la récupération du token d'authetification.
