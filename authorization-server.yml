#Spécifier le port
server:
  port : 9000
  servlet :
    context-path : "/auth"

### Client Details ######
client-details :
  clientId : test
  clientSecret : test
  grantTypes : authorization_code
  scopes : scope
  redirectUris : http://localhost:9999/login
  tokenValiditySeconds : 3600
  resourceIds : testId
